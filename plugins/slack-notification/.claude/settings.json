{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'dir=$(pwd); while [ \"$dir\" != \"/\" ] && [ ! -d \"$dir/.claude\" ]; do dir=$(dirname \"$dir\"); done; if [ -d \"$dir/.claude\" ]; then cd \"$dir\" && .claude/slack-notify.sh complete; fi'"
          }
        ]
      }
    ]
  }
}
